package it.tecnet.crs.util;

import it.tecnet.crs.mod.service.ModellazioneServiceImpl;

public class Test {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		/*try
		{
			
			String cronExpression = "0+0+0+1+1+%3F+*";
			boolean b = org.quartz.CronExpression.isValidExpression(URLDecoder.decode(cronExpression));
			System.out.print(b);
			Driver d = (Driver)Class.forName("com.microsoft.sqlserver.jdbc.SQLServerDriver").newInstance();
			String DB_URL = "jdbc:sqlserver://192.0.2.24\\mssql2012;databaseName=DB_CruscottoAudit;user=sa;password=sa";
			java.sql.Connection con = DriverManager.getConnection(DB_URL);
			con.close();
		}catch (Exception e) {
			e.printStackTrace();
		}*/
		/*ModellazioneServiceImpl servizio = new ModellazioneServiceImpl();
		Area areaBean = new Area();
		areaBean.setStato('A');
		areaBean.setDescrizione("PROVA AREA");
		servizio.saveArea(areaBean);*/
	}

}
